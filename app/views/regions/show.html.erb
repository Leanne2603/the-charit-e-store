<p id="notice"><%= notice %></p>
<div class="jumbotron">
    <div class="row">
        <div class="col-md-6">
            <h1 class="display-4">Region Details</h1>
        <p>
        <strong>Region:</strong>
        <%= @region.region %>
        </p>

        <%= link_to 'Edit Region', edit_region_path(@region) %> |
        <%= link_to "Delete Region", region_path(@region), method: "delete", data: { confirm: "Are you sure" } %>
        <br>
        <br>
        <%= link_to 'Back', regions_path %>

        <div class="col-md-6">
                <%= image_tag "map", class: "home-image" %>
        </div>
    </div>
</div>
